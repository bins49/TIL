### 콜백과 비동기 실행

- 파라미터는 함수에 전달받은 값을 함수 내부로 전달하기 위해 사용하는 변수
- 아규먼트는 함수에 실제로 전달되는 값을 의미한다

#### 콜백이란?

- 콜백은 **어떤 함수의 argument로 또 다른 함수를 전달하는 것을 의미한다.**
  - 다른 함수에 인자로 전달되서 전달된 함수가 실행되는 동안 나중에 호출된다
  - 쉽게 말해, 어떤 일이 끝난 후에 실행될 함수를 미리 정해둔다. 

- `import` or `export` 같은 ES Moudle 문법을 사용하기 위해, `package.json` 파일에 type을 추가한다.

```js
{
  "type" : "module"
}
```



- 콜백 예시

```js
function sayHello() {
  console.log("Hello World!");
}

function sayGoodbye() {
  console.log("Goodbye World!");
}

function printMessage(func) {
  console.log("Printing message...");
  func();
}

// Hello World! 출력
printMessage(sayHello);
```

- 간단한 거는 Arrow function을 사용하면 된다.

```js
printMessage(() => console.log("Hello World!"));
```

- 다른 콜백 예시

```js
function sayHello(name) {
  console.log(`hello ${name}`);
}

function sayGoodbye(name) {
  console.log(`Goodbye ${name}`);
}

function printMessage(func, name) {
  console.log("Printing message...");
  func(name);
}

// hello Bob 출력
printMessage(sayHello, "Bob");
// Arrow Function 활용
printMessage((name) => console.log(`Goodbye ${name}`, "Bob"));
```



#### 콜백과 비동기 함수

- 특정 시간이 지난 다음에 어떤 함수를 실행해 주는 `setTimeout`이라는 함수가 있다. 이게 비동기 프로그램의 예시이다.

- 예시

```js
// 차례대로 1, 3, 2를 출력한다.

console.log("1")

setTimeout(() => console.log("2"), 3000);

console.log("3")
```

- 비동기 프로그램이 동기 프로그램보다 더 빨리 끝난다. 비동기 프로그램이 훨씬 효율적이다.
- 비동기 함수(Asynchronous Function)
  - **함수의 내용을 끝까지 쭉 실행하지 않고 중간에 다른 작업을 처리하다가 다시 돌아와서 마무리를 하는 함수**

- `setInterval()`함수는 시간 간격을 두고 콜백을 반복적으로 실행한다.

```js
console.log('Start');

// setInterval(callback, interval): interval 단위는 밀리초입니다.
setInterval(() => console.log('2초가 지났습니다'), 2000);

console.log('End');

// 출력 결과
// Start
// End
// 2초가 지났습니다.
// 2초가 지났습니다.
// 2초가 지났습니다.
```

- `clearInterval()`함수를 사용하면 `setInterval()`함수를 해제시킨다.

```js
console.log("Start");

const intervalID = setInterval(() => console.log("2초가 지났습니다."), 2000);

// 7초후에 setInterval() 해제
setTimeout(() => clearInterval(intervalID), 7000);

console.log("End");
```



#### 콜백 헬

- 콜백 안에 콜백에 여러 번 중첩되는 것을 **콜백 지옥**이라고 부른다. 

- 예시

```js
setTimeout(() => {
  console.log('첫 번째 작업');
  setTimeout(() => {
    console.log('두 번째 작업');
    setTimeout(() => {
      console.log('세 번째 작업');
      // 이렇게 계속 중첩될 수 있음...
    }, 1000);
  }, 1000);
}, 1000);
```





### Promise

- 콜백 지옥을 해결하기 위해 ES6부터는 Promise를 사용한다.  

- fetch 함수가 Promise라는 걸 리턴해서 그 값이 response에 저장한다. 

```js
const response = fetch("www.naver.com/api/employees");
console.log(response);
```

- Promise란
  - 비동기 작업이 완료되면 값을 알려주는 객체이다.
  - 작업이 완료되면 값을 알려줄 것을 약속한다. 
  - **일단 Promise를 돌려주고 나중에 작업이 완료되면 결과값을 Promise에 채워 넣어준다. **

- 이전에 사용한 방식(콜백 지옥)

```js
getEmployee((response) => {
  json(response, (data) => {
    console.log(data);
  });
});
```

- Promise를 사용한 방식

```js
const response = await fetch(url);
const data = response.json();
console.log(data);
```

- Promise를 다루는 방법은 크게 2가지이다.
  - 1. `.then()`메소드 + 콜백
  - 2. `async`와 `await` 문법



#### await

- 비동기 작업이 완료되면 Promise가 결괏값을 알려주는데 그 결괏값을 받아오려면 await문법을 사용한다. 
- 사용 예시

```js
const response = await fetch("www.naver.com/api/employees");
// json 메소드도 시간이 걸릴 수 있어 
const data = await response.json()
console.log(data);
```

- **Promise가 바로 할당되는 것이 아니고 fetch 작업이 완료될 때까지 기다렸다가 fetch의 결괏값이 리스폰스에 할당된 것이다. **
- json 메소드도 시간이 걸릴 수 있어서 promise를 먼저 리턴하기 때문에, 웬만하면 `await`를 앞에 붙어서 json 작업이 끝난 후에 할당될 수 있게 해준다. 
- Promise는 3가지 중 하나의 상태를 가지게 된다. 
  - 1.pending
    - 비동기 작업의 결과를 기다리고 있을 때 
  - 2.Fulfilled
    - 비동기 작업이 성공적으로 완료됐을 때
  - 3.Rejected
    - 비동기 작업이 중간에 실패할 경우

#### async

- 기본적으로 `await`은 `async` 함수 안에서만 사용 가능하다. 
- `async`는 보통 `function` 앞에 정의된다.
- 예시

- 예시

```js
export async function printEmployee() {
  const response = await fetch(api);
  const data = await response.json();
  console.log(data);
}

// Arrow function 사용시
const printEmployee = async () => {
  const response = await fetch(api);
  const data = await response.json();
  console.log(data);
}
```



#### 효율적인 비동기 코드 

```js
async function printEmployees() {
  for (let i = 1; i < 11; i++) {
    const response = await fetch(`https://learn.codeit.kr/api/employees/${i}`);
    const data = await response.json();
    console.log(data);
  }
}

printEmployees();
```

- 위의 코드는  수많은 await문을 순차적으로 실행한다. 그래서 비효율적이다

- 개선된 코드
  - 이전보다 시간도 더 빨리 끝난다.
  - 다만 각 리퀘스트를 거의 동시에 보내기 때문에 순서가 뒤죽박죽이다. 

```js
async function printEmployees(i) {
    const response = await fetch(`https://learn.codeit.kr/api/employees/${i}`);
    const data = await response.json();
    console.log(data);
}

for (let i = 1; i < 11; i++) {
		printEmployees(i);
}
```



#### try catch로 오류 처리하기

- 다양한 리퀘스트 오류 상황을 고려해서 적절히 오류 처리를 해줘야 한다. 
- 사용 예시

```js
export async function printEmployees() {
  try {
    const response = await fetch(`https://learn.codeit.kr/api/employees/`);
    const data = await response.json();
    console.log(data);
  } catch (error) {
    console.log(error);
    return;
  } finally {
    console.log("Finished"); 
  }
}
```

- try catch문과 상관없이 끝에 무조건 실행해야 하는 코드가 있다면 finally를 활용하자



#### Promise와 오류 제대로 이해하기

- `Fullfilled` 상태면 Promise 값을 꺼내서 return 해준다. 
- **`Rejected` 상태면 Promise 값을 throw한다. 오류를 throw해도 try 안에 있기 때문에 잡아서 처리해 준다. **





#### then 메서드

- 핵심
  - 비동기 작업이 끝나면 `.then()` 메소드에 등록되어 있는 콜백이 성공 결괏값을 가지고 실행된다. 

- 예시코드

```js
fetch("https://learn.codeit.kr/api/employees")
	//reponse가 돌아오면 아래 콜백을 실행하고
	.then((response) => response.json());
	// response.json이 완료되면 이 콜백을 실행한다.
	.then((data) => console.log(data));
```

- 코드 순서
  - 1. `fetch`함수는 URL로 리퀘스트를 보내고 곧바로 Promise를 리턴한다.
  - 2. 기다리다가 리퀘스트가 완료돼서 response가 돌아오면 첫 번째 Promise는 Fulfilled 상태가 된다. 
  - 3. Promise가 Fulfilled 상태가 되면 then 메소드에 등록된 콜백이 실행된다. 
  - 4. 이때 Promise의 결괏값을 콜백의 첫 번째 파라미터로 전달한다. 그래서 response가 콜백으로 전달되는 것이다.
       1. 만약에 콜백이 Promise를 리턴하면 then 메소드가 리턴한 Promise도 동일한 상태와 결괏값을 갖게 된다. 
       2. 콜백이 Promise가 아닌 평범한 값이면, then 메소드가 리턴한 Promise는 Fulfilled 상태가 되고 리턴 값을 결괏값으로 갖게 된다.
  - 5. Fulfilled 상태가 되면 다음 then 메소드에 등록된 콜백이 실행된다. 파싱 결과가 첫 번째 파라미터로 전달된다. 

- Promise chain의 중요한 점은 **비동기 작업을 기다리는 동안에 다른 코드를 먼저 실행한다.**



#### .catch()와 .finally() 메소드

- `.then`메서드와 Promise chain을 사용할 때 `catch`, `finally`를 사용할 수 있다.
- `.catch` 메소드는 Promise chain에서 발생하는 오류를 잡아서 콜백을 실행해 준다.
- `.finally` 메소드는 Promise chain에서 어떤 일이 발생하든 끝에 콜백을 실행해준다.

- 사용 예시

```js
fetch("https://learn.codeit.kr/api/employees")
	.then((response) => response.json());
	.then((data) => console.log(data));
	.catch((error) => console.log("Error!"));
  .finally(() => console.log("Finished"));
```

- `.catch`메메서드는 앞서는 프로미스 체인 어딘가에서 오류가 났을 때 실행할 콜백을 등록하면 된다. 
  - 오류가 어디서 나든 Rejected 상태의 Promise가 계속 전파되는 원리 때문에 catch 메소드가 오류를 처리할 수 있다. 그래서 보통 `.then()` 메서드 뒤에 사용한다. 



#### Promise.all() 메소드

- `Promise.all()`은 여러 Promise를 활용하는 비동기 작업 여러 개를 병렬적으로 처리하고 싶을 때 사용한다. 
- 예시 코드

```js
async function getEmployee(id) {
    const response = await fetch(`https://learn.codeit.kr/api/employees/`);
    const data = await response.json();
    return data;
}

const promises = [];

for (let i = 1; i < 11; i++) {
  promises.push(getEmployee(i));
}

Promise.all(promises);
```

- 코드 순서
  - 1. 처음에는 배열에 있는 Promise들이 Pending 상태이다.
  - 2. Argument로 전달된 Promise들이 모두 Fulfilled 상태가 되면 Promise도 Fulfilled 상태가 되고 각 Promise의 성공 결괏값들로 이루어진 배열을 성공 결괏값으로 갖게 된다. 

- await 및 try & catch 사용시

```js
async function getEmployee(id) {
    const response = await fetch(`https://learn.codeit.kr/api/employees/`);
    const data = await response.json();
    return data;
}

const promises = [];

for (let i = 1; i < 11; i++) {
  promises.push(getEmployee(i));
}

let employees;

try {
  employees = await Promise.all(promises);
} catch (error) {
  console.log("Error!");
}

console.log(employees);
```

